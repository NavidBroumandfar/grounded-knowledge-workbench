# Asteria Pump Systems: Review Register

This fictional reviewer-only record contains quality observations used to test
access boundaries. It must never appear in retrieval context for `reader` or
`engineer` roles.

The current synthetic observation is that calibration evidence must be attached
before a maintenance case can be approved.
