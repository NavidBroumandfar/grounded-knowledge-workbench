# Executable notebook

The end-to-end training notebook will be added after the first cited retrieval
vertical slice is reproducible from the command line. The notebook will call
the package API rather than contain an independent implementation.
